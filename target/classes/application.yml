spring:
  jpa:
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:postgresql://localhost:5433/towerdb
    username: postgres
    password: postgres


  liquibase:
    change-log: classpath:/liquibase/changelog-master.xml

